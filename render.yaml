services:
  - type: web
    name: escala-web
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn escala_project.wsgi"
    envVars:
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true